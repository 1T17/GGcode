// Legitimate recursion that should work fine

function factorial(n) {
    if (n <= 1) { 
        return 1 
    }
    return n * factorial(n - 1)
}

// This should work normally
let result = factorial(5)